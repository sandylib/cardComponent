import React from 'react';
import ReactDOM from 'react-dom';


it('renders without crashing', () => {
  const section = document.createElement('BackgroundImage');
  ReactDOM.render(<section />, section);
});
